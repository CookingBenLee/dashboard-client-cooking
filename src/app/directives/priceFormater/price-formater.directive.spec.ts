import { PriceFormaterDirective } from './price-formater.directive';

describe('PriceFormaterDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceFormaterDirective();
    expect(directive).toBeTruthy();
  });
});
