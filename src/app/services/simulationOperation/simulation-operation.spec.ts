import { SimulationOperation } from './simulation-operation';

describe('SimulationOperation', () => {
  it('should create an instance', () => {
    expect(new SimulationOperation()).toBeTruthy();
  });
});
