import { CategoryOperation } from "../categoryOperation/category-operation";
import { Simulation } from "../simulation/simulation";

export class SimulationOperation {
    id: number;
    refTypeOperation: CategoryOperation;
    montant: number;
    refSimulation: Simulation;
  }
